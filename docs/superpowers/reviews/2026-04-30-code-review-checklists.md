# Code Review Checklists (2026-04-30)

Status semantics rule: plan contract tracks `pass`/`fail`/`unknown`; this execution uses `Pass`/`Partial`/`Fail` where `Partial` is treated as `unknown` with anchored partial evidence. Impact triage (`Blocker`/`High`/`Medium`/`Low`) is tracked separately in the report severity model.

## Feature Completion Checklist

| Item | Status | Notes |
| --- | --- | --- |
| CLI command surface | Pass | Verified help surfaces for root + workspace/sub/export/profile/template/asset/config. Evidence: `./build/subcli --help` at `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-3-1-root-help`, plus subcommand probes `./build/subcli workspace --help` (`#probe-3-1-workspace-help`), `./build/subcli export --help` (`#probe-3-1-export-help`), and related `#probe-3-1-*` command/output blocks. |
| Workspace precedence | Partial | Mixed evidence: runtime default path and invalid env override rejection are directly probed (`./build/subcli doctor --json` -> `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-3-2-doctor-default`; `SUBCLI_WORKSPACE="/tmp/subcli-review-ws" ./build/subcli doctor --json` -> `#probe-3-2-doctor-env-invalid`; `./build/subcli workspace status --json` -> `#probe-3-2-workspace-status`), and unit/smoke tests assert precedence ordering (`rg -n "resolveEnvironment|EnvironmentResolveInput|SUBCLI_WORKSPACE|resolution_source" tests/subcli_tests.cpp tests/cli_smoke.sh src/main.cpp` -> `#probe-5-4-test-gap-anchor-map`). No direct runtime probe transcript yet for successful env override in this review pass, so keep Partial. |
| Export guarantees | Partial | Partial evidence exists via direct command/source anchors: `./build/subcli export mihomo --profile bypass-cn --sub explain --json` and `./build/subcli export all --profile bypass-cn --sub explain --strict-capabilities --json` are covered in `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-5-4-test-gap-anchor-map` (`tests/cli_smoke.sh:244`, `tests/cli_smoke.sh:250-251`, `src/main.cpp:3687`); missing direct probes remain for empty-selection and zero-node-failure contracts (`src/main.cpp:3482`, `src/main.cpp:3529`). |
| Profile schema behavior | Pass | Traceable at `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-3-3-source-refs` with named assertions: `src/profile.cpp:178-179` (`default_outbound` parse path), `tests/subcli_tests.cpp:587,607` (`default_outbound` override assertions), `tests/subcli_tests.cpp:243-273` (`template_policy` parse matrix), and `tests/subcli_tests.cpp:302-303,332-333,362-363` (invalid target/path/action rejection assertions). |
| Capability warnings | Pass | Source anchors confirm `capability_unsupported`/`capability_degraded` emission in exporters and strict-capabilities block JSON contract in CLI path. Evidence: `src/exporter_*.cpp` + `src/main.cpp` refs in evidence log. |
| Release claims | Partial | Claim map includes direct release/packaging probes (CMake version/install/CPack lines, docs claim scan, package target run) at `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-6-1-cmake-release-packaging`, `#probe-6-3-release-claim-scan`, and `#probe-6-4-package-target-run`; external dependency for release confidence remains export guarantees failure-contract closure anchored at `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-5-4-test-gap-anchor-map` (`src/main.cpp:3482`, `src/main.cpp:3529`). |

## Boundary Condition Checklist

| Item | Status | Notes |
| --- | --- | --- |
| Invalid YAML parse/contracts | Pass | Verified malformed fixture path via `./build/subcli --workspace ... sub validate bad-yaml` block at `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-4-1-validate-bad-yaml`. |
| Invalid JSON parse/contracts | Pass | Verified malformed fixture path via `./build/subcli --workspace ... sub validate bad-json` block at `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-4-2-validate-bad-json`. |
| Invalid base64 parse/contracts | Pass | Verified malformed fixture path via `./build/subcli --workspace ... sub validate bad-base64` block at `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-4-3-validate-bad-base64`. |
| Invalid URI parse/contracts | Pass | Verified malformed fixture path via `./build/subcli --workspace ... sub validate bad-uri` block at `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-4-4-validate-bad-uri`. |
| Missing assets/templates/profiles | Pass | Verified via `./build/subcli --workspace ... template validate --json` (`#probe-4-5-template-validate-json`), `./build/subcli --workspace ... profile validate /tmp/.../no-such-profile.json` (`#probe-4-5-profile-validate-missing`), and `./build/subcli --workspace ... asset validate` (`#probe-4-5-asset-validate-missing`). |
| Invalid workspace path/marker | Pass | Verified via `./build/subcli --workspace /tmp/.../not-a-dir doctor --json` (`#probe-4-6-workspace-path-invalid`) and `./build/subcli --workspace /tmp/.../ws workspace doctor` (`#probe-4-6-workspace-marker-warn`). |
| Permission denied writes | Pass | Verified via `chmod 444 /tmp/.../ws/config.yaml` + `./build/subcli --workspace /tmp/.../ws config set timeout 9` at `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-4-7-config-write-permission-denied`. |
| Network failure/timeout | Pass | Verified via `./build/subcli --workspace /tmp/.../ws sub update net-fail --strict-network` block at `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-4-8-sub-update-strict-network-timeout`. |
| Duplicate/invalid sub headers/tags | Fail | Mixed behavior shown by `./build/subcli --workspace ... sub list --json` duplicate-tags block (`#probe-4-9-duplicate-tags-preserved`) and invalid header rejection command `./build/subcli --workspace ... sub add --name bad-header ... --header 'NoColonHeader' --force` (`#probe-4-9-invalid-header-rejected`). |
| CLI parse/usage errors | Pass | Verified with direct usage/error probes: `./build/subcli --workspace ... export` (`#probe-4-10-export-arity-usage`), `./build/subcli --workspace ... template set sing-box normal` (`#probe-4-10-template-set-arity`), `./build/subcli --workspace ... config get` (`#probe-4-10-config-get-arity`), and `./build/subcli --workspace ... sub list --bogus-flag` (`#probe-4-10-sub-list-unknown-flag`). |

## Redundancy / Maintainability Checklist

| Item | Status | Notes |
| --- | --- | --- |
| File-size hotspot control | Fail | `src/main.cpp` is 4393 lines, with large command handlers (`doSubCommand` 562 lines, `doExportCommand` 523 lines, `doConfigCommand` 431 lines). Evidence: `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-5-1-line-count-hotspots` and `#probe-5-1-main-function-lengths`. |
| Command orchestration complexity | Partial | Dense `if (cmd == ...)` branch chains are repeated across help/dispatch surfaces in `src/main.cpp`, increasing change-surface risk. Evidence: `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-5-2-dispatch-branch-density`. |
| Warning/error formatting reuse | Partial | Warning emission patterns are duplicated across sub/export/runtime paths rather than centralized helper formatting. Evidence: `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-5-2-warning-duplication`. |
| Broad catch concentration | Partial | Catch-all handlers exist in parser and top-level CLI (`src/parser.cpp:23`, `src/main.cpp:1108`, `src/main.cpp:4389`), reducing diagnostic precision. Evidence: `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-5-3-catch-concentration`. |

## Test Gap Checklist

| Item | Priority | Status | Notes |
| --- | --- | --- | --- |
| Parser edge inputs | High | Partial | Invalid YAML/JSON/base64/URI inputs are covered in Task 4 runtime probes and parser-focused tests, but export-failure paths tied to parser-empty outcomes remain missing as direct assertions (`no nodes parsed from enabled subscriptions`). Evidence: `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-4-1-validate-bad-yaml`, `#probe-4-4-validate-bad-uri`, and `#probe-5-4-test-gap-anchor-map`. |
| Workspace lifecycle | High | Pass | Unit tests cover precedence ordering (CLI > env > marker > persisted) and smoke tests assert `SUBCLI_WORKSPACE` doctor JSON source/paths + workspace init/status/doctor flows. Evidence: `tests/subcli_tests.cpp:5662`, `tests/subcli_tests.cpp:5702`, `tests/subcli_tests.cpp:5723`, `tests/subcli_tests.cpp:5748`, `tests/cli_smoke.sh:173`, `tests/cli_smoke.sh:264`, and anchor map `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-5-4-test-gap-anchor-map`. |
| Export strict capability | High | Pass | Smoke coverage asserts strict-capability blocked JSON contract (`"strict_capabilities_blocked":true` + `"violations"`) and non-JSON strict failure path. Evidence: `tests/cli_smoke.sh:250`, `tests/cli_smoke.sh:251`, `tests/cli_smoke.sh:262`, and anchor map `docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-5-4-test-gap-anchor-map`. |
| CLI usage error contracts | Medium | Partial | Representative wrong-arity/unknown-flag evidence captured (`export`, `template set`, `config get`, `sub list --bogus-flag`), but explicit exit-code assertions are still missing for full contract closure. |
| Release packaging metadata | Medium | Partial | Evidence now captured for project version + CPack generator + install payload (`docs/superpowers/reviews/2026-04-30-code-review-evidence.md#probe-6-1-cmake-release-packaging`), release workflow presence check (`#probe-6-2-release-workflow-presence`), docs claim scan (`#probe-6-3-release-claim-scan`), and package artifact generation (`#probe-6-4-package-target-run`). Residual release-domain gap: no `release*` automation workflow present under `.github/workflows`; external dependency for release confidence remains export failure-contract closure tracked in report actions. |
